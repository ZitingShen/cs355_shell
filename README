README

Not-supported functionality:
1. consecutive ;s
2. consecutive |s
3. multiple redirection
4. parentheses in cmdline
5. for piping, all jobs need to be in either foreground or background.
6. bg, fg, kill, jobs, exit and history cannot be executed in background.

Extra features:
1. tab completion
2. history
3.  a. for commands "fg" and "bg", support execution with first argument of command, if only one FG, BG or ST job in the 
       joblist. (e.g. "fg sleep" valid if only one "sleep xx" running/stopped in the joblist).
	b. Also, will print out "bg: sleep: ambiguous job spec" if more than one job starts with the given command string.
